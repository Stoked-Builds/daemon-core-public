# Backlog

Canonical work items for this project.

## Structure
- `epics/` — Large initiatives
- `features/` — Feature-level work items
- `stories/` — User stories
- `tasks/` — Individual tasks

Use this directory when following the full backlog model.
For exploratory work, use `../ideas/` instead.
