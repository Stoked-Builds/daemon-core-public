---
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description?: string;
}

<<<<<<< HEAD
const { title, description = 'Stux OS - A modular operating system for agentic computing' } = Astro.props;
=======
const { title, description = 'Stux OS – Run your own AI operating system.' } = Astro.props;
>>>>>>> f01fc51 (Initial release: Stux OS and DaemonCore website)
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body>
    <Navbar />
    <main>
      <slot />
    </main>
    <Footer />
  </body>
</html>

<style is:global>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  :root {
    --color-bg: #0a0a12;
    --color-bg-elevated: #12121f;
    --color-bg-card: #1a1a2e;
    --color-text: #e0e0e8;
    --color-text-muted: #8888a0;
    --color-accent: #6366f1;
    --color-accent-hover: #4f46e5;
    --color-border: #2a2a4a;
    --font-sans: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
    --font-mono: 'SF Mono', 'Fira Code', 'Fira Mono', Menlo, monospace;
  }

  html {
    font-family: var(--font-sans);
    background: var(--color-bg);
    color: var(--color-text);
    line-height: 1.6;
  }

  body {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }

  main {
    flex: 1;
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
    width: 100%;
  }

  a {
    color: var(--color-accent);
    text-decoration: none;
    transition: color 0.2s;
  }

  a:hover {
    color: var(--color-accent-hover);
  }

  h1 {
    font-size: 2.5rem;
    color: var(--color-text);
    margin-bottom: 1.5rem;
    letter-spacing: -0.02em;
  }

  h2 {
    font-size: 1.5rem;
    color: var(--color-text);
    margin: 2rem 0 1rem;
  }

  h3 {
    font-size: 1.25rem;
    color: var(--color-text);
    margin: 1.5rem 0 0.75rem;
  }

  p {
    margin-bottom: 1rem;
  }

  ul, ol {
    margin: 0 0 1rem 1.5rem;
  }

  li {
    margin-bottom: 0.5rem;
  }

  code {
    font-family: var(--font-mono);
    background: var(--color-bg-card);
    padding: 0.2rem 0.4rem;
    border-radius: 4px;
    font-size: 0.9em;
  }

  pre {
    background: var(--color-bg-card);
    padding: 1rem;
    border-radius: 8px;
    overflow-x: auto;
    margin: 1rem 0;
  }

  pre code {
    background: none;
    padding: 0;
  }

  strong {
    color: var(--color-text);
  }
</style>
